to start backend :
cd ./movies_api
then run command :pip install -r requirements.txt
to start the apis run command :uvicorn main:app --host 0.0.0.0 --port 10000


to start streamlit :
cd ./streamlit
run command : streamlit run main.py
